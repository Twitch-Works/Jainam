{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter=@jainam/api... run build",
  "framework": null,
  "regions": ["bom1"],
  "functions": {
    "api/index.ts": { "maxDuration": 60 }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
