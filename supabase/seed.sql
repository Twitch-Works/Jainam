-- Content is seeded programmatically so it can stay authored in TypeScript
-- (typed, reviewable, re-runnable):
--
--     pnpm db:seed        # runs apps/api/src/seed/run.ts against SUPABASE_URL
--
-- `supabase db reset` runs this file automatically; the seeder is separate so
-- it can also target a hosted project. Nothing to do here.
select 1;
